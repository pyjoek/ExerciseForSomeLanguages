#include <stdio.h>

int main() {
    int n = 90;
    char letter = 'K';
    char name[] = "joel";

    printf("The integer is %d \n", n);
    printf("The letter is %c \n", letter);
    printf("The word is %s", name);
}